# Formatted manuscript output

The `gh-pages` branch hosts this directory at <https://$OWNER_NAME.github.io/$REPO_NAME/>.
The permalink for this version is <https://$OWNER_NAME.github.io/$REPO_NAME/v/$COMMIT/>.

The manuscripts in this directory were built from [`$COMMIT`](https://github.com/$REPO_SLUG/commit/$COMMIT).
